# TradeHelper 2.0 设计入口

> 2.0 的详细实施顺序、模块合同和测试验收标准以 [V2_REFACTOR_PLAN.md](./V2_REFACTOR_PLAN.md) 为准。1.x 设计文档已归档到 [docs/archive/v1/DESIGN_V1.md](./docs/archive/v1/DESIGN_V1.md)。

## 1. 核心目标

TradeHelper 2.0 要把系统从“策略先出信号，预测再做降级”的旧流程，重构为更接近专业交易决策的流程：

当前决策链：

```text
数据事实 -> 特征快照 -> 预测模型 -> 情景规划器
  -> 策略引擎 -> 风控官 -> 单股/组合决策 -> 报告/UI
```

共享验证链：

```text
TradePlan + ExecutionDecision
  -> 当前订单预览 / 历史成交仿真 / 到期事实验证
    -> 预测账 / 策略账 / 联合账 -> 学习闭环
```

两条链必须共享冻结事实、TradePlan 和风控结果。当前建议与历史回放只允许成交适配器不同，不能维护两套信号逻辑。

TradeHelper 一以贯之的产品目标，是稳定回答五个问题：

1. 现在是否可以买、卖、减仓、加仓、持有？
2. 如果现在不能操作，达到什么条件可以操作？
3. 如果判断错了，最大亏损是多少，在哪里失效？
4. 这个建议过去有没有正期望，可信度有多高？
5. 系统预测的是哪个目标日期、概率和收益区间，过去预测到底准不准？

系统要能分清：

1. 预测模型是否准确。
2. 预测准确时，交易策略是否赚钱。
3. 最终经过风控后的联合建议是否有正期望。
4. LLM 提出的新观察是否值得沉淀为候选特征或候选策略。

系统只生成可复现的决策计划和订单预览，不连接券商自动下单；用户始终是最终执行者。任何“可执行”都表示证据和风险闸门允许，不代表保证盈利。

## 2. 文档职责

| 文件 | 职责 |
|------|------|
| [README.md](./README.md) | 项目入口：当前目标、状态和命令 |
| [DESIGN.md](./DESIGN.md) | 架构设计：系统是什么、为什么这样分层、各层边界是什么 |
| [V2_REFACTOR_PLAN.md](./V2_REFACTOR_PLAN.md) | 实施计划：先做哪一层、交付什么、怎么测试、阶段状态如何 |
| [docs/V1_CAPABILITY_INVENTORY.md](./docs/V1_CAPABILITY_INVENTORY.md) | V1 能力资产清单：哪些能力必须迁移、V1 在哪里、V2 去哪里、用什么测试保护 |
| [docs/v2/CONTRACTS.md](./docs/v2/CONTRACTS.md) | V2-0/V2-1 规范合同：精确类型、不变量、序列化和 repository 边界 |
| [docs/v2/POLICIES.md](./docs/v2/POLICIES.md) | V2-0/V2-1 确定政策：数据源、时段、缓存、质量、数据库和常量 |
| [docs/v2/GOLDEN_CASES.md](./docs/v2/GOLDEN_CASES.md) | V2-0/V2-1 标准答案：固定输入、预期输出和测试映射 |
| [docs/v2/V2_2_FEATURES.md](./docs/v2/V2_2_FEATURES.md) | V2-2 规范：point-in-time 特征合同、公式、缺失语义、存储和 Golden Cases |
| [docs/v2/V2_3_FORECAST.md](./docs/v2/V2_3_FORECAST.md) | V2-3 规范：预测目标、标签、模型、OOF、注册、持久化和 Golden Cases |
| [docs/v2/V2_4_SCENARIOS.md](./docs/v2/V2_4_SCENARIOS.md) | V2-4 规范：多周期情景、当前事实覆盖、三时段、策略家族政策和 Golden Cases |
| [docs/v2/V2_5_STRATEGIES.md](./docs/v2/V2_5_STRATEGIES.md) | V2-5 规范：TradePlan、条件 DSL、首批模板、V1 迁移矩阵和 Golden Cases |
| `AGENTS.md` | Codex 本地约定：Codex 开发时先读它，再读 DESIGN 和 V2_REFACTOR_PLAN |
| `CLAUDE.md` | Claude Code 本地约定：Claude 开发时先读它，再读 DESIGN 和 V2_REFACTOR_PLAN |

V2-0/V2-1 实现发生冲突时，规范优先级为：`CONTRACTS/POLICIES/GOLDEN_CASES` > 本设计和实施计划中的示例 > V1 能力清单 > V1 参考代码。V2-2 至 V2-5 分别以对应阶段规范为准。未被规范的行为应保持缺失或明确不支持，不能由实现者自由补默认值。

## 3. 分层职责

| 层 | 职责 | 不负责 |
|----|------|--------|
| 数据层 | 标准化行情、新闻、基本面、账户和数据质量 | 生成交易结论 |
| 特征层 | 生成 point-in-time FeatureSnapshot | 偷看未来数据 |
| 预测层 | 输出明确目标日的概率和收益区间 | 直接下单 |
| 情景层 | 把预测翻译成交易环境 | 计算仓位 |
| 策略层 | 按情景生成 TradePlan | 越过风控执行 |
| 风控层 | A/B/C/D 分级、仓位和亏损约束 | 发明交易点子 |
| 成交仿真层 | 将同一 TradePlan 转为订单意图，模拟触发、费用、滑点和成交 | 修改预测或策略结论 |
| 组合决策层 | 对 Tab3 候选计划做跨股票排序、风险分配和替换比较 | 重新计算单股信号 |
| 学习层 | 预测账、策略账、联合账和自优化 | 无验证地改生产逻辑 |
| LLM 假设层 | 提出研究假设和质疑 | 直接生成可执行指令 |
| UI/报告层 | 让用户理解结论、条件、风险和证据 | 反推业务逻辑 |

## 4. 不可丢失的 V1 硬约束

2.0 可以重构架构，但不能丢掉 1.x 期间反复踩坑后形成的业务约束。

完整 V1 能力迁移对照表见 [docs/V1_CAPABILITY_INVENTORY.md](./docs/V1_CAPABILITY_INVENTORY.md)。任何标记为“必须迁移”的能力，未经用户明确确认不得删除。

### 4.1 数据源原则

| 数据 | A 股 | 美股 |
|------|------|------|
| 历史日 K | TickFlow；失败时明确不可用 | Nasdaq 历史 OHLCV -> yfinance -> TickFlow，仅接收已完成交易日 |
| 盘中实时 | TickFlow | TickFlow |
| 延伸时段 | 不适用 | Nasdaq.com -> yfinance |
| 基本面 | baostock -> akshare；LLM 仅可解释有来源的事实 | Finnhub -> yfinance -> akshare -> 百度可验证页面；LLM 仅可解释有来源的事实 |
| 新闻 | 东方财富 -> akshare | Finnhub 个股新闻 + 市场新闻 |
| 上市日期 | baostock | Finnhub profile2 |

硬规则：

1. 所有降级必须记录来源、时间戳和原因。
2. 上市日期必须限制缓存读取、网络请求、回测窗口和预测训练窗口。
3. 盘中实时价和延伸时段价只能进入当前决策快照，不得写入正式日 K。
4. Tab1 和 Tab3 可以共用缓存，但不能互相依赖；两者都必须独立刷新行情、新闻和基本面。
5. A 股和美股必须在数据合同、测试 fixture、报告和 UI 中同等覆盖。
6. 所有时间必须带市场、交易所、时区和交易会话语义；正式目标日必须由交易所日历计算。
7. 日 K 必须声明复权口径；拆股、分红和代码变更不能被误判为普通价格波动。
8. LLM 不得补造缺失的财务数字。没有可靠来源时必须保留缺失状态。
9. Provider 没有返回的字段必须保存为缺失，不能用 0 冒充。数据质量按具体策略需要的字段分级，价格可用不代表成交量或盘中形态可验证。
10. Tab3 按股票隔离数据质量；单股缺价只阻断该股票，不能污染或阻塞其他成分股。

### 4.2 Tab1 和 Tab3 边界

| 页面 | V2 定位 | 必须额外考虑 |
|------|---------|--------------|
| Tab1 单股分析 | 单只股票完整研究 | 行情、技术、基本面、新闻、预测、情景、策略、风控、历史证据、LLM 观察 |
| Tab3 我的持仓 | 组合级交易工作台 | 真实账户余额、持仓股数、成本价、现金、浮盈浮亏、单票集中度、组合相关性、关注股替换、禁止加仓、减仓/止盈/止损 |

Tab3 不是 Tab1 批量版。Tab3 输出必须优先回答组合层面的处理顺序：先处理哪些持仓风险，哪些股票可以继续持有，哪些关注股值得替换，账户还能承受多少新增风险。

### 4.3 决策合同

策略层不能只返回一条买卖信号。每次分析必须生成一个 `StrategyBundle`：

```text
StrategyBundle:
  scenario_id
  position_state
  entry_or_add
  reduce_or_exit
  hold
  invalidation
  conservative_plan_ids[]
  aggressive_plan_ids[]
  conflict_state
```

其中每个 `TradePlan` 必须包含动作意图、触发条件、触发价、结构止损、止盈类型、缺失条件、失效条件和计划有效期。`TradePlan` 不包含股数、仓位比例、账户权益或最大亏损金额；V2-6 的 `ExecutionDecision` 必须使用真实账户权益和持仓计算执行等级、股数、仓位与最大亏损。保守与激进计划共享同一组事实和预测方向，V2-5 只允许确认门槛不同；风险预算和仓位差异由 V2-6 负责，不允许生成互相矛盾的市场判断。

盘前、盘中、盘后都必须输出买入/加仓条件、卖出/减仓条件、持有条件和失效条件：

| 模式 | 决策口径 | 计划有效期 |
|------|----------|------------|
| 盘前 | 盘前快照 + T-1 正式日 K | 当日常规交易会话 |
| 盘中 | 当前实时快照 + T-1 及以前正式日 K | 当日剩余交易会话 |
| 盘后 | 当日正式收盘数据 | 下一交易日常规交易会话 |

### 4.4 执行等级

| 等级 | 标准 | 含义 |
|------|------|------|
| A | 事实成立、风险可控、当前股票/策略有可靠正期望证据 | 可执行 |
| B | 事实成立、风险可控，但样本或证据不足 | 小仓验证 |
| C | 事实成立，但历史期望、风险容量或预测一致性不支持执行 | 仅观察 |
| D | 数据冲突、关键事实缺失或计划不可成交 | 驳回 |

风险退出独立于新开仓等级：止损、锁利和集中度减仓不能因为预测偏多或缺少预测 Champion 而被阻止。

LLM 观察无论被系统确认、反驳、待验证或因数据无效无法判断，都必须保留并展示证据；风控官只决定能否执行，不决定用户能否看到。

### 4.5 自优化边界

- 预测模型、策略模板和联合建议分账评估，不能互相冒领效果。
- 自动优化只允许调整注册模型、特征子集、受控参数、权重和执行软阈值，不能自动改写生产 Python 源码。
- 止损、最大亏损、账户权益、数据质量、A股 T+1 等硬约束不可被收益优化取消。
- 没有模型通过验证时，系统仍要输出基线概率、未通过原因和条件观察计划；“结果完备”不等于强行给出可执行买入。
- 负期望候选应降级、回滚或停止交易，系统不承诺收益单调上升，只晋升样本外证明确实改善的候选。

### 4.6 A股/美股一致性要求

任何 V2 阶段验收都必须回答：

1. 这个能力是否同时支持 A 股和美股？
2. 如果市场规则不同，差异是否集中在市场规则/数据适配层，而不是散落在业务逻辑里？
3. 是否有 A 股和美股 fixture 或测试覆盖？
4. 报告是否明确显示数据来源、市场、交易时段和可执行日期？

## 5. 代码组织原则

2.0 初期保持项目清爽：

- 根目录只保留当前有效入口文档。
- 旧版文档统一归档到 `docs/archive/v1/`。
- V2 新代码优先进入独立 `tradehelper_v2/` 包，不直接散落到 V1 的 `core/`、`services/`、`report/`、`strategies/` 中。
- 1.x 目录在迁移期作为参考实现、算法来源和回归对照；V2 主链路不直接 import V1 的耦合业务模块。
- 复用的是 V1 中验证过的处理逻辑、算法思想、数据源经验和测试案例，不是把 V1 代码直接搬进 V2。
- 若极少数外部 I/O client 需要临时借用，必须用显式 compatibility shim 包住，并在对应 V2 阶段计划里写清替换目标和退出条件；预测、策略、风控、学习主链路不得依赖这些 shim。
- 每层必须有自己的测试，端到端测试不能替代单层测试。

## 6. 当前阶段

当前下一阶段：

```text
V2-6 风控层
```

V2-0 至 V2-5 已完成并复审。V2-6 只能按 `docs/v2/V2_6_RISK.md` 使用 StrategyBundle、真实账户、冻结估值、数据质量、历史证据和市场规则生成 ExecutionDecision；不得修改 TradePlan，也不得提前实现 OrderIntent、成交仿真、组合分配、学习、LLM 或 UI。
