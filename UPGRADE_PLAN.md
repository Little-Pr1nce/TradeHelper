# TradeHelper 可信交易建议系统升级方案

> 本文档是 2026-06-25 汇总确认的主线升级方案，并持续记录实施结果。当前状态更新至 2026-07-05：LLM 发现机会，代码验证事实，风控官分级执行，历史数据让系统自我升级。前半部分保留原始目标，实际完成度以“当前落地状态”和文末五阶段表为准。

## 一、最终目标

系统要能稳定回答五个问题：

1. 现在是否可以买、卖、减仓、加仓、持有？
2. 如果现在不能操作，达到什么条件可以操作？
3. 如果判断错了，最大亏损是多少，在哪里失效？
4. 这个建议过去有没有正期望，可信度有多高？
5. 系统预测的是哪个目标日期、概率和收益区间，过去预测到底准不准？

报告形式要从：

```text
买入 / 卖出 / 观望
```

升级成：

```text
独立预测 + 当前状态 + 条件触发计划 + 风险金额 + 失效条件 + 预测/方案双重历史验证 + LLM观察确认状态
```

## 二、系统角色边界

| 角色 | 职责 | 输出 |
|------|------|------|
| 预测系统 | 基于时点数据预测明确目标日的方向概率和收益区间 | 可复现 ForecastResult |
| 策略/代码系统 | 结合预测、历史证据和当前事实生成交易条件 | 可复现 TradePlan |
| LLM 研究员 | 发现系统没覆盖的机会、解释报告、质疑系统判断 | 观察候选/解释 |
| 风控官 | 检查事实一致性、风险可控性、历史有效性 | 执行等级 |
| 用户 | 最终决策者 | 执行/忽略/反馈 |

关键原则：

- LLM 不直接生成交易指令。
- LLM 可以提出观察，不应该被系统静默丢弃。
- 风控官不决定“看不看”，只决定“能不能执行”。
- 用户能看到 LLM 和系统的分歧。

## 三、风控官执行等级

| 等级 | 标准 | 动作 |
|------|------|------|
| A | 事实成立 + 风险可控 + 历史正期望 | 可执行 |
| B | 事实成立 + 风险可控 + 历史样本不足 | 小仓验证 |
| C | 事实成立但风险/历史不支持 | 仅观察 |
| D | 数据冲突 | 驳回 |

检查标准：

- 事实一致性：是否真的触碰 MA120、创新高、放量、冲高回落、跌破支撑。
- 风险可控性：是否有止损位，最大亏损是否低于账户权益阈值，仓位是否过高。
- 历史有效性：同类形态后续 1/3/5/10 日收益、胜率、最大不利波动。

## 四、LLM 观察候选池

新增“研究员观察 vs 系统确认”章节：

| 股票 | LLM观察 | 系统状态 | 执行级别 | 下一步 |
|------|------|------|------|------|
| GLW | 冲高后回落，适合锁利 | 已确认 | 可执行 | 跌破锁利线部分止盈 |
| FCX | 多次触碰 MA120 后反弹 | 待验证 | 小仓验证/仅观察 | 重新站回 MA120 后验证 |
| SNDK | 动量很强可追 | 系统反驳 | 不执行 | Alpha偏空且波动过高 |

LLM 点子处理流程：

1. LLM 提出观察。
2. 系统抽取成结构化模板。
3. 风控官检查事实、风险、历史。
4. 报告分级展示，不静默删除。

## 五、盘中/盘前/盘后报告逻辑

| 报告类型 | 决策方式 |
|------|------|
| 盘中 | 使用实时价，输出当日可执行条件 |
| 盘前 | 使用盘前价 + T-1 数据，预测开盘后触发条件 |
| 盘后 | 使用收盘数据，生成下一交易日条件计划 |

三类报告都必须输出：

- 买入/加仓触发条件
- 卖出/减仓触发条件
- 持有条件
- 失效条件
- 保守方案
- 激进方案

## 六、Tab1 与 Tab3 定位

| 页面 | 定位 |
|------|------|
| Tab1 单股分析 | 单只股票的完整策略、技术、基本面、新闻和条件计划 |
| Tab3 我的持仓 | 组合视角，优先处理已有持仓、账户余额、关注列表、调仓优先级 |

Tab3 不是 Tab1 批量版。Tab3 必须额外考虑：

- 当前仓位
- 成本价
- 浮盈浮亏
- 可用现金
- 单票集中度
- 是否禁止加仓
- 是否需要减仓/止盈/止损
- 哪些关注股值得替换现有持仓

## 七、策略系统优化

方案提出时 `strategies/` 不够完备；当前五类关键策略和统一接口均已落地，以下问题保留作为改造依据。

原始问题及当前状态：

- 只输出 buy/sell/no_signal：已改为原生 `StrategyDecision` 和缺失条件诊断。
- 盘中实时价未进入策略核心：已通过内存 `decision_df` 重算当前 Decision。
- 偏买入、退出不足：已增加 Q/R/S 和独立退出质量复盘。
- 缺 MA120、冲高锁利策略：已落地 P/Q。
- `risk_budget` 语义混乱：已统一为账户风险金额、仓位上限和市场规则成本。
- I/J/K/L/M/N 噪音：已纳入审计、健康度和执行降级；长期实盘降噪仍继续观察。
- H/O 重复：O 已明确为趋势满仓对标基准，不作为普通默认推荐。

已新增：

| 策略 | 作用 |
|------|------|
| MA120SupportReboundStrategy | 半年线支撑反弹（策略 P） |
| ProfitLockAfterHighStrategy | 冲高回落锁利（策略 Q） |
| PositionRiskManagementStrategy | 持仓止盈、减仓、止损、禁止加仓（策略 R） |
| PullbackFailedExitStrategy | 跌破关键均线后反抽失败退出（已落地为策略 S） |
| ConditionalTriggerStrategy | 统一生成条件触发计划（已落地为策略 T） |

策略接口升级为：

```python
StrategyDecision:
    action: buy / sell / hold / watch / invalid
    execution_level: A / B / C / D
    shares
    price_type
    trigger_price
    stop_loss
    take_profit
    take_profit_pct
    take_profit_mode: fixed / dynamic / conditional / none
    take_profit_rule
    max_loss_amount
    position_pct
    invalidation
    missing_conditions
    reason
    time_stop_days
    hard_stop_pct
```

## 八、已有待优化项合并

| 优先级 | 项目 | 当前状态 |
|--------|------|----------|
| P0 | 历史预测评估 UI | 已有 Tab3 面板；全局筛选、钻取和图表仍待完善 |
| P0 | 独立预测与策略双闭环 | **核心链路已落地**；独立 ForecastEngine、三张新表、OOF 参数晋升、预测/方案分账和 Tab1/Tab3 展示已完成，校准图、漂移回滚和组合策略 OOF 重放仍待加固 |
| P0 | LLM 建议边界 | 已有观察候选、结构化确认和分歧展示；更多形态模板待补 |
| P0 | 数据完整度/样本不足降级 | 已接入数据质量、因子覆盖、审计和历史证据闸门 |
| P0 | 实时价重算风控 | 已使用内存 `decision_df`，不污染历史 K 线 |
| P0 | 策略审计置信区间 | 已加入 400 次循环分块 Bootstrap |
| P0 | 数据适配器完善 | 核心链路和延伸时段保守流动性代理已稳定；停复牌/ST 与可选授权 Level 2 适配器仍待补 |
| P1 | Final_Score 权重反馈 | 已按行情动态权重并接入因子验证覆盖；长期反馈仍需观察 |
| P1 | 新闻质量/缓存失效 | 已按时段 TTL、发布时间和刷新状态治理 |
| P1 | 回测/实盘口径隔离 | 已区分历史 `df` 与当前 `decision_df` |
| P1 | 持仓状态细分 | 已支持持有、减仓、止盈、止损和禁止加仓 |
| P1 | 策略参数自动调优 | 已有候选、确认、晋升、回滚和 recovery 闭环 |
| P1 | A 股盘前/盘中 | 盘中已接 TickFlow；A 股无独立盘前连续交易价 |
| P2 | 异常数据质量评分 | 已落地 OHLC、上市日期、实时新鲜度和新闻质量检查 |
| P2 | 市场规则集中化 | 已落地 `utils/market_rules.py` |
| P2 | 统一置信度评分 | 已由可信度硬摘要和 A/B/C/D 执行等级实现核心能力 |

P3：

- Web 版完善
- 打包体积优化

## 九、自我升级闭环

这是让风控官变聪明的核心。

每次出现一个形态，都记录：

- 股票
- 日期
- 市场状态
- 形态类型
- 触发价
- 止损价
- 后续 1/3/5/10 日收益
- 最大不利波动
- 是否触发止盈
- 是否触发止损
- 当时执行等级
- LLM 是否提出过该观察

之后系统可以统计：

- 某形态在某股票上是否有效
- 某形态在某市场状态下是否有效
- LLM 提出的观察命中率
- 策略实际正期望
- 哪些规则应该升级/降级

最终形成：

- 规则模板库
- 形态表现数据库
- 策略健康度面板
- 历史预测评估 UI 面板
- 自动降权/升权机制

## 十、报告结构优化

最终报告固定结构：

1. 独立预测：目标日期、概率和收益区间
2. 组合/单股概览
3. 当前可执行动作
4. 不同预测情景下的条件交易计划
5. 持仓风控提示
6. 关键支撑/突破/止盈观察
7. 研究员观察 vs 系统确认
8. 策略回测与历史验证
9. 风险金额和仓位建议
10. 保守方案
11. 激进方案
12. 失效条件
13. 预测能力与策略盈利能力双重评估

这样报告不会变成一大堆分析文字，而是交易员真正能用的操作手册。

## 十一、实施顺序

### 第一阶段：把现有报告升级成条件触发计划

- 已完成当前价重算、买/卖/持有/失效条件和 GLW/FCX 类支撑/锁利案例覆盖。
- Tab3 已有分阶段进度与全宽报告；组合汇总会移除逐股重复策略表，HTML 导出的长表默认折叠并可按需展开。

### 第二阶段：升级 strategies/

- 五类 P-T 策略已落地。
- 20 个注册策略已支持 `watch/missing_conditions` 和原生未触发诊断。
- 风险预算语义已统一；回测与实盘/报告使用同一 `StrategyDecision -> Order` 路径。
- 执行层按策略家族只保留最高质量代表，退出共识按独立家族计票，避免多个相似趋势策略重复放大同一观点。

### 第三阶段：加入 LLM 观察候选池

- LLM 候选、结构化解析、系统兜底和 Tab1/Tab3 分歧展示已落地。
- 已覆盖锁利、MA120、动量追高、假突破、超卖反转和趋势回调等事实校验及 A/B/C/D 分级。
- LLM 提出的形态与系统规则形态分账统计命中率；系统规则成功不能替 LLM 冒领成绩。历史负期望会自动降级；历史正期望只有在当前事实已确认、触发价和止损有效且方向关系一致时才能把 C 提升到 B，不能替 LLM 补齐缺失的风险定义。
- Tab3 历史评估已展示总样本、LLM 样本和 LLM 5日胜率；剩余是独立的全局筛选/钻取交互，不影响当前风控闭环。

### 第四阶段：历史验证与自我升级

- 形态记录、买入/退出复盘、Tab3 历史评估、策略健康度和自动升降级均已落地。
- 参数具有候选确认、晋升、负期望 recovery 和回滚闭环。
- 交易方案按实际可执行会话归组，并按供应商/补充分钟证据质量跨交易日折算胜率、有效样本和 Wilson 置信下界；有效样本少于8时不能标记为可靠。相同方案重复运行不会虚增样本，策略版本、账户快照、价格、止损、止盈、仓位或盘中生成分钟变化则保留为可审计的新版本。
- 预测 Challenger 已扩展为相似行情、正则化多分类、平滑浅层概率树和集成模型；所有模型使用相同标签口径，选择/确认窗口之间按预测周期增加标签成熟隔离带，并经配对时间块 Bootstrap 改进下界、Log Loss、ECE 和80%区间覆盖率闸门后才能成为 Champion。方向概率和收益区间必须来自同一个加权条件分布。
- walk-forward 候选采用双通道：至少半数样本外窗口可以凭正基准超额收益通过，也可以在牛市保留至少80%基准收益、回撤降低30%且夏普高0.2后以风险调整优势通过；基础正收益、交易数和非负夏普仍是硬要求。
- 候选还需至少 3 个不同截止日确认及 20 天影子观察，中途失效会清零确认和观察时钟。策略健康章节展示 OOS 收益、超额收益、合格窗口、晋升通道和影子状态。

### 当前 P0：独立预测与策略双闭环（2026-07-03 确认）

#### 当前落地状态（2026-07-05）

- 已新增 `utils/trading_calendar.py`，正式目标日使用 XNYS/XSHG 交易所日历；依赖不可用时不静默生成正式预测。
- 已新增独立 `ForecastResult` 和 `core/forecast_engine.py`，输出 1/3/5 交易日上涨/震荡/下跌概率、收益 P10/P50/P90、特征哈希和模型版本，不读取买卖动作。
- 已新增 `forecast_log`、`forecast_model_versions`、`trade_plan_log`。预测和方案分别按稳定事件键去重，到期只补录结果；旧 `prediction_log` 已停止新增并退出策略健康度计算。
- 已实现目标日正式收盘验证、方向正确率、Brier、Log Loss、ECE、区间覆盖率，以及交易方案扣成本表现、MFE/MAE、风险退出机会成本的独立评价。盘中方案只允许使用冻结信号时点之后的独立分钟K复盘，缺少证据时保持待验证，禁止用整日K伪造路径。
- 后台预测优化已按“股票+市场+周期”隔离；较早40个 OOF 点选参数、最近40个独立确认，中间按周期加入标签成熟隔离带。两个窗口都必须优于历史频率 Brier/Log Loss 基线、配对时间块 Bootstrap 的90%改进下界为正、ECE 不得明显恶化且80%区间覆盖率至少70%。v4 之前的 Champion 不再复用，必须重新通过 OOF；真实到期样本满20次后若 Brier 劣于基线5%以上，Champion 自动回滚退出执行链。
- `StrategyContext` 已接入冻结预测，`signal_intent` 已区分建仓、普通退出、风险退出、锁利和计划；偏空预测只会降低新开仓等级，不得阻止风险退出。
- Tab1/Tab3 报告已固定“独立预测在前、交易方案在后”；历史评估页分开展示新版预测能力和交易方案表现。LLM 只能解释代码预测，不能另造概率或把卖出动作解释为看空。
- 已新增无未来数据、预测/方案冻结去重、跨股票模型隔离、基线晋升、在线回滚、明确目标日验证、风险退出隔离、双账评价和管道串联测试。
- 2026-07-03 已备份并清理本机旧口径数据：停止保留动作派生预测、旧观察、旧报告、旧回测缓存和旧 `audit_pass` 参数；删除8只股票受周末日K污染的整段缓存，保留持仓/关注/余额/新闻和结构正常K线；SPCX 有效上市日起点纠正为 2026-06-11。
- 2026-07-04 已新增 `core/joint_oof.py`：每个测试折只用此前训练段选择预测邻居数、策略参数变体和策略家族，测试段逐日通过正式 `StrategyDecision -> Order -> Broker`、数据质量、预测降级、执行等级、组合仓位和逐步累积的历史健康度完成联合回放。结果独立保存到 `joint_oof_runs`，不再用单策略全样本回测冒充最终建议链表现。
- 预测评价已补齐 Log Loss、top-label ECE、置信度分箱校准曲线和行情状态分层；模型版本保存 Log Loss/ECE，Tab3 历史评估同时展示单独预测能力和最终联合策略 OOF 收益、基准、超额收益、夏普及回撤。
- 样本至少40个决策点且完成至少5笔交易后，联合OOF负收益且负超额会降低新开仓等级和仓位；至少8笔且收益、超额、夏普均明显为负时只保留观察。该闸门不能升级买入，也不能阻止止损、锁利等风险退出。
- 分钟K能力已完成实测：TickFlow 0.1.22 SDK 有历史 `1m` 接口，但当前美股和A股令牌均返回“无分钟K线查询权限”；官方文档同时说明历史分钟线主要面向A股。免费替代只能作为近期补充：美股 yfinance 分钟数据最多回看60天，A股 AKShare/东方财富提供分钟历史接口，但都不满足长期、稳定、可审计的统一回放要求。
- 2026-07-05 联合 OOF 升级为 `joint_oof_v4_embargo_coherent`：每个测试折分别用训练段选择 1/3/5 日预测模型与参数，模型选择采用标签成熟隔离和概率/收益同源分布；测试点同时生成三周期概率并通过正式风险链路。结果按周期保存 Brier、Log Loss、ECE、区间覆盖率、准确率及样本数。当前信号使用 50%/30%/20% 的已验证多周期共识，周期冲突只会降级买入，未验证预测不参与偏向，任何预测都不能阻止风险退出。
- 已新增独立 `intraday_price_history`：TickFlow 有权限时优先，失败后美股使用 yfinance 近期 1 分钟、A股使用 AKShare/东方财富 1 分钟；统一交易所时区、正式交易时段、A股午休、来源、抓取时间和质量状态。只验证信号时间之后的前瞻数据；A股盘中买入按下一分钟入场，当日只记录路径，下一交易日才允许退出，符合 T+1。
- 盘中方案的冻结时点已从“行情快照时间”收紧为“报告实际生成时间”，用户看到建议前已经发生的行情不计入策略收益。分钟K采用质量优先写入，补充源不得覆盖已有 TickFlow 供应商数据；每次方案验证记录证据来源、质量和实际使用的K线数量，并在历史评估中公开展示。
- Tab3 历史评估新增真实概率校准图、联合 OOF 分周期指标和分钟证据覆盖摘要；分钟样本会随 Tab1/Tab3 盘中运行逐步积累，不把工程链路完成误写成已经拥有充分统计证据。
- 已新增 `feature_context_snapshots`，Tab1/Tab3 各自把当次真实可见的新闻得分、发布时间、来源、基本面字段、供应商和抓取时点冻结；相同内容去重、内容变化保留新版本，禁止按新闻发布日期把后来抓取的数据回填到过去。历史评估公开显示覆盖起止和来源，覆盖不足时继续排除出预测 OOF。
- 联合 OOF 现在保存逐事件的预测发生日、目标日、预测方向、实际方向、对错、策略 Decision 以及 Broker 的真实成交/拒单结果；漂移只允许在相同 `policy_version` 内纵向比较，新版本首轮只建立基线，不与旧算法误比。有效漂移告警只降低新开仓等级和仓位。

P0/P1 工程范围已完成。真实分钟K与新闻/基本面快照仍需随用户运行自然积累；这属于证据成熟度，不用历史回填伪造。覆盖不足时系统保持待验证或继续排除相应特征。

#### 1. 目标与边界

旧链路把买入直接记为“预测上涨”、卖出直接记为“预测下跌”，预测只是策略建议的别名。目标链路改为：

```text
时点数据
  -> ForecastEngine 独立预测未来市场状态
  -> 回测/审计给策略提供历史证据
  -> 策略结合预测概率生成多情景 TradePlan
  -> 风控官应用不可被优化取消的硬约束
  -> 分别验证预测是否正确、方案是否盈利
  -> 预测模型与策略参数各自晋升、降级和回滚
```

关键原则：

- 预测不从最终买卖动作反推；风险减仓、盈利锁定和集中度调整不等同于看跌预测。
- 预测系统评价“目标日市场结果是否判断正确”；策略系统评价“真实触发后扣成本是否赚钱”。
- LLM 只能解释预测、提出观察和质疑，不能成为正式预测模型或直接修改交易方案。
- 自动优化只调整模型版本、权重和受控参数，不允许系统自行改写 Python 策略源码。
- 止损、账户风险、仓位集中度、数据质量等硬风控不能因为追求收益被优化掉。
- 系统不承诺准确率或盈利逐次单调上升；目标是只晋升样本外确实改善的候选，无法证明正期望时自动降级或不交易。

#### 2. ForecastResult 与预测周期

新增独立 `ForecastResult`：

```text
stock_code / market / mode
generated_at / data_cutoff
target_session_date / horizon
reference_price
prob_up / prob_flat / prob_down
expected_return_p10 / p50 / p90
confidence / market_regime
model_version / feature_snapshot_hash
```

- 统一输出当日收盘、+1、+3、+5 个交易日预测；不同 `mode`、市场和周期分别统计，不混为一个正确率。
- 目标日期必须在预测生成时冻结。先完善交易日历服务，覆盖美股交易所假日和 A 股休市日；当前仅跳过周末的实现不能用于正式目标日。
- 正式预测以概率和收益区间表达，不伪造确定性目标价。报告用天气预报式完整语句展示预测时间、明确目标日、参考价、实际收盘价和对错。

#### 3. 预测模型与无泄漏训练

- 首个 Champion 使用透明、可复现的概率模型；技术趋势、动量、波动率、成交量和市场状态作为首期特征。
- 首期历史训练只允许使用具有完整时点历史的字段。新闻和基本面必须在建立 point-in-time 快照后才能加入历史训练，禁止用当前财务数据或后来抓取的新闻回填过去。
- 每个历史预测必须按以下顺序生成：

```text
只训练到 T-1
  -> 生成并冻结 T 时点 ForecastResult
  -> 策略只读取该份预测
  -> T 之后到期验证
```

- 预测模型使用 walk-forward 的 out-of-fold 预测评价，禁止用全样本训练后的模型重新预测训练期。
- 必须与“永远看涨”、历史上涨基准率和简单动量基准比较，不能只看表面准确率。

预测评价指标：方向准确率、Brier Score、Log Loss、概率校准误差、收益区间覆盖率、Rank IC，以及按市场状态/周期拆分的稳定性。

#### 4. 策略与预测的组合

`StrategyContext` 增加对应周期的冻结预测，但预测只作为输入证据，不直接生成订单：

```text
forecast_id
prob_up / prob_flat / prob_down
expected_return_interval
forecast_confidence / forecast_model_version
```

- 趋势、突破、均值回归等方向策略根据预测概率和自身触发条件生成主方案与备用方案。
- `signal_intent` 明确区分 `alpha_entry`、`alpha_exit`、`risk_exit`、`profit_lock`、`rebalance`；只有方向性动作进入预测正确率。
- 策略回测必须使用当时已经冻结的样本外预测，不能调用今天的 Champion 回放过去。
- 不选单一“全市场最好策略”，而是按市场、周期、行情状态和预测概率区间选择稳健策略家族。

策略评价指标：真实触发率、扣成本净期望、胜率、Sharpe、Calmar、最大回撤、MFE/MAE、机会成本、避免损失，以及相对基准/风险调整优势。

#### 5. 双重验证与归因

| 预测结果 | 方案结果 | 反馈对象 |
|------|------|------|
| 正确 | 盈利 | 增加预测和策略证据 |
| 正确 | 亏损 | 只惩罚策略、入场或风控参数 |
| 错误 | 盈利 | 降低预测模型权重，保留策略容错证据 |
| 错误 | 亏损 | 预测和策略分别降权 |

未触发方案不计交易盈亏，但记录机会成本；风险退出按避免损失和过早退出成本评价，不进入上涨/下跌准确率。

#### 6. 自动优化和版本治理

预测与策略分别采用 Champion/Challenger：

```text
候选 -> walk-forward 样本外验证 -> 多截止日确认
     -> 影子运行 -> 显著优于 Champion -> 晋升
     -> 表现恶化/漂移 -> 自动回滚
```

- 预测优化不能只追逐准确率，必须同时改善概率评分和校准；策略优化不能只追逐收益，必须满足回撤和风险约束。
- 样本按“市场 + 周期 + 行情状态”分层；单股样本不足时使用市场级先验，达到最低样本后才允许股票级微调。
- 每次模型和参数晋升保存版本、训练窗口、特征集合、指标、随机种子和数据哈希，确保可复现。
- 市场漂移触发降权或回滚，不允许每次运行报告都即时改参数造成震荡。

#### 7. 数据库与迁移

新增四张表：

- `forecast_log`：不可修改的正式预测、目标日、概率、区间、模型版本和到期结果。
- `forecast_model_versions`：Champion/Challenger、训练窗口、指标、状态、晋升和回滚原因。
- `trade_plan_log`：关联 `forecast_id` 的方案、账户快照、触发/成交、盈亏、MFE/MAE、机会成本和归因结果。
- `joint_oof_runs`：最终“预测+策略+风控官”联合策略的训练/测试折、交易绩效、预测校准、行情分层和审计轨迹。

现有 `prediction_log` 标记为 legacy 建议验证记录，保留审计，不直接混入新 ForecastEngine 的模型训练和正式准确率。

#### 8. UI 与报告

- “预测能力”面板：按市场/模式/周期展示预测内容、目标日期、实际结果、准确率、Brier、校准曲线和模型版本。
- “策略盈利能力”面板：按策略/市场状态/预测分桶展示触发数、净期望、回撤、机会成本和参数版本。
- “预测 × 策略归因”面板：展示预测正确/错误与方案盈利/亏损的四象限。
- 报告固定先展示独立预测，再展示不同预测情景下的交易计划，最后展示预测证据和策略证据；LLM 解释放在确定性内容之后。

#### 9. 实施顺序与验收

1. 交易日历、`ForecastResult`、三张表和旧记录隔离。
2. 1/3/5 日透明预测基线、冻结目标日期和天气预报式报告。
3. walk-forward 历史预测重放与预测能力面板。
4. `StrategyContext` 接入预测，增加 `signal_intent` 并分离风险退出。
5. 回测消费 out-of-fold 预测，建立 TradePlan 双重验证。
6. Forecast/Strategy 各自 Champion/Challenger、影子运行和回滚。
7. Tab1/Tab3 端到端历史重放、跨模式和旧数据库迁移测试。

完成验收必须同时满足：目标日期生成时已冻结；历史预测可复现且无未来数据；预测优于简单基准；策略只用当时预测；预测和方案可独立归因；任何自动晋升均可审计和一键回滚；无正期望证据时系统明确输出不交易。

### 第五阶段：完善数据质量和市场规则

- 数据质量、上市日期裁剪、因子覆盖和逐股实时价闸门已落地。
- A/US 市场规则、税费、动态滑点、跳空止损和基础流动性约束已集中化。
- K 线与新闻按时段失效，实时/延伸报价不写历史收盘。
- 美股延伸时段没有 Level 2 时启用保守流动性代理：有买一/卖一则按价差把新开仓压到 25%~75%，只有成交量则压到 50%，只有最新价则压到 25%；报告明确标记代理口径，不把 top-of-book 冒充完整深度。
- 可靠停复牌/ST 数据仍待完善；完整美股深度只能作为需授权订阅的可选数据源接入。

#### 分钟K复盘的可信替代路径（2026-07-05 已落地）

1. 分钟数据独立写入 `intraday_price_history`，不进入日K `price_history`；每条记录保留市场时区时间戳、交易日、来源、抓取时间和质量状态。
2. 优先使用具有历史分钟权限的 TickFlow；当前令牌无权限后本进程不再重复请求，转为美股 yfinance 近期分钟数据或 A股 AKShare/东方财富分钟数据。失败时保持待验证，不伪造证据。
3. 只保留正式交易时段，过滤美股盘前盘后及 A股午休；必须等目标交易时段完成后才评价。A股当日买入不能当日退出，下一交易日才检查止盈止损。
4. 只验证冻结信号时间之后采集到的分钟K。历史缺口不反向填造，也不使用整日 high/low 猜测盘中触发顺序；当前剩余工作是积累真实样本和接入可选授权数据源。

## 十二、一句话总结

这套优化不是让 LLM 更小，也不是让代码更死板，而是建立一个完整闭环：

```text
预测系统判断未来环境，策略系统制定方案，风控官分级执行，历史结果分别优化预测与策略。
```

## 当前落地状态

截至 2026-07-05，五阶段主链路均已落地，未完成项集中在证据覆盖、LLM 形态库和数据适配深度：

- 已新增 `StrategyDecision`。
- 已完成 `StrategyDecision -> Order` 统一转换：回测引擎和当前信号检查都从 `StrategyDecision` 生成订单，避免回测与报告两套并行信号逻辑漂移。
- 已新增 MA120 支撑、冲高回落锁利、持仓风控、反抽失败退出、统一条件触发计划五类策略。
- 已让当前信号检查使用实时价快照。
- 已让 Tab3 输出组合级条件触发计划、持仓风控提示、关键支撑/止盈观察。
- 已加强 LLM 只能解释系统方案的边界。
- 已新增 `services/research_observations.py`，支持解析 LLM 观察候选、系统兜底观察、事实校验、A/B/C/D 分级和报告展示。
- 已在 Tab1/Tab3 报告中追加「研究员观察 vs 系统确认」章节，让用户看到 LLM/研究员观察与代码系统确认/反驳之间的分歧。
- 已新增 `research_observation_log` 形态观察表，开始记录观察形态、触发价、止损、执行等级、LLM 来源，并自动验证后续 1/3/5/10 日收益和最大不利波动。
- Tab3 报告已能展示「观察形态历史表现」摘要：样本数、5日胜率、5日/10日均值、最大不利波动和正期望状态。
- 风控官已开始使用观察形态历史表现修正执行等级：历史负期望会降级；历史正期望只能增强已经完成事实确认和止损定义的观察，不能把缺少风险条件的 LLM 点子越级成小仓验证。
- Tab3 已新增「历史预测评估」UI 面板：用户可主动刷新查看组合、持仓/关注股票的历史预测正确率、平均实际收益、期望状态，以及观察形态表现。
- 风控官已开始使用策略历史健康度作为硬闸门：`demote` 的买入/加仓策略不再生成执行方案，只能观察；`watch` 策略会自动降级并减半仓位。
- 策略健康度已加入样本置信度闸门：按 Wilson 95% 正确率下界、样本数和平均实际收益共同判断 `keep/watch/demote`；小样本不能直接可靠，负期望策略会被降级，健康度观察信号会进一步压缩仓位。
- 负期望策略已接入自动优化闭环：历史健康度判为 `demote` 的策略会写入 `per_stock_params` 降级记忆；后续策略池不再信任旧参数，但仍允许全量候选重新参加 walk-forward。若新参数样本外重新转正，会以 `auto_tuned` 覆盖 `demoted`，恢复为新的候选参数。
- 负期望恢复机制已进一步增强：对 `demoted` 策略自动生成更保守的 `*_recover_*` 参数候选，例如提高趋势/动量入场阈值、提前退出、收紧止损、降低投入比例；这些恢复候选必须通过 walk-forward 样本外验证，才允许覆盖旧的负期望记忆。
- 已新增数据质量评分与交易可信度闸门：OHLC 异常、非正价格、样本不足、日期异常、实时价偏离、缺新闻/基本面/盘口会影响评分；严重问题阻断新开仓，降级问题压缩仓位并降低执行等级。
- Tab3 组合方案已展示「数据质量概览」，用户能看到每只股票的评分、状态、交易闸门、仓位倍率和降级原因。
- Tab1/Tab3 页面已做信息层级重排：Tab1 已从堆叠式页面改为「分析工作台 + 全宽报告阅读」模式，输入、时段提示和运行状态集中在分析前，生成报告后自动切到包含 K 线图和报告正文的阅读区；Tab3 已从左右分栏改为「编辑工作台 + 全宽报告阅读」模式，账户持仓、关注股票、历史评估用顶部页签切换，生成报告后自动切到全宽阅读区。Tab3 报告开头新增「一分钟操作台」，先给出退出、风控、买入、支撑观察、暂不操作的数量和处理顺序，再展开条件触发表。
- Tab3 持仓录入已支持行内编辑：已有持仓可以直接修改股数和成本价，保存后复用原有持仓更新逻辑；不再需要先删除再重新录入。
- Tab1/Tab3 报告顶部已新增「可信度硬摘要（代码生成）」：统一展示数据质量闸门、A/B/C/D 信号等级、样本外策略审计、历史预测期望、策略健康降级和硬约束提醒。数据阻断会直接显示 D，负期望/策略降级会直接显示 C，不再把可信度问题埋在报告尾部。
- 可信度硬摘要已改为“当前机会作用域”：组合中无关股票或无关策略的历史降级只作为背景提示，不再把整份报告一票降为 C；只有当前可执行信号对应的同股票、同策略证据才能形成硬阻断。
- 历史预测验证已升级为 v2 口径：使用预测参考交易日后的第 N 个交易日收盘价，不再误用区间 `MAX(close)`；记录标的原始涨跌、方向调整净收益、验证价、MFE/MAE、入场触发、止损/止盈路径和验证截止日，并扣除估算交易摩擦。
- 策略健康度只使用 v2、已触发、可验证的建议，按净盈利率和方向净收益升降级；旧 v1 记录会在 K 线充足时自动重算，无法恢复的旧组合记录会标记为不支持，不再参与自动学习。
- 组合预测已保存现金和真实持仓快照，后续按第 N 个交易日组合净值验证，不再以 `predicted_price=0` 生成无意义结果。
- 盘中决策已使用内存临时 K 线：保留最后正式收盘日，追加实时 OHLCV，重算 MA/MACD/RSI/布林/KDJ、技术 Alpha 和 `StrategyDecision -> Order -> 风控`；临时 K 线不会写入历史数据库，也不会重复执行耗时回测。
- 参数自动优化已升级为候选生命周期：停用全样本“最优参数”扫描和报告展示；walk-forward 通过的参数先进入候选，必须跨不同数据截止日连续确认，且样本外收益、夏普和交易数同时达标后才能晋升。冠军参数实盘健康度转负时会标记回滚并退出正式参数，重新进入完整策略池竞争。
- Tab1/Tab3 策略健康章节已展示参数候选的观察中、已晋升、已替代、已回滚和未通过状态，以及确认次数、OOS 收益/夏普和交易数，自动升级不再是后台黑箱。
- Tab3 已新增组合风险预算：计算单票仓位、股票总仓位、HHI 集中度、近 90 日收益相关性、估算年化波动和剩余新增容量；资金分配会受 25% 单票上限、35% 高相关组合上限和 90% 股票总仓位上限直接约束。
- 市场规则进一步实盘化：A 股按主板/创业板/科创板/北交所应用 10%/20%/30% 涨跌幅规则，支持显式 ST 5% 标记、T+1、5 元最低佣金、卖出印花税；部分卖出后保留剩余持仓，回测盈亏按分批退出累计。
- Tab1/Tab3 前台运行已改为“正式参数快速分析 + 后台深度优化”：前台继续执行正式参数回测、策略审计、健康度闸门和交易计划，但不再同步等待几十组参数变体与 walk-forward；报告返回后由单线程后台任务依次完成深度优化，同股票同数据截止日不会重复提交。
- 同板块比较已切换为因子筛选管道，只计算技术指标、Alpha、行情状态和排名，不再为 10 只同类股重复运行策略回测/审计/参数池。本地 260 日样本基准从约 5 秒/只降至约 0.27 秒/只。
- Tab3 已将报价、K线和基本面改为有限并发预取，权益估算和逐股分析复用同一份数据；避免报价、持仓估值和正式分析三段重复请求。
- 正式策略回测已接入 SQLite 缓存，并把账户资金与 Final Score 序列签名加入缓存键，既避免误复用又显著加速重复分析。本地基准同一截止日第二次前台量化从约 4.03 秒降至约 0.13 秒。
- LLM 输入已压缩：Tab3 每只股票只发送验证质量靠前的 3 个策略摘要，不再发送多笔完整交易流水；Tab1 每策略只保留最近一笔行为示例，并收紧最大输出长度和超时，减少模型端等待与冗长报告。
- 已加固 K 线缓存读写入口：日 K 写入 SQLite 前会剔除未完成交易日数据、基础 OHLC 异常和疑似复权/单位异常跳变尾部数据；读取旧缓存时也会忽略已经污染的尾部数据并尝试从稳定尾部补拉，避免坏数据被后续报告误当成收盘价。
- 已新增市场规则集中模块，操作方案最大亏损开始包含估算滑点/佣金/税费；回测引擎基础交易规则改为从集中规则读取。
- 2026-06-29 完成 P0 可信度加固：因子 IC/IR 审计只能调整当前时点，不得回写历史分数；预测可靠性改为对最新信号向零收缩，不再被归一化抵消；新闻情绪按日历日真实半衰。主管道现已启用因果安全的 IC/IR 校验，只按已实现的历史收益调整最新决策行，历史回测行保持当时可得的等权分数。
- Tab3 真实权益为 0 时现在会硬阻断新开仓，不再使用虚构 10 万元；持仓未录入建仓日期时，不再把买入前的全周期高点当成持仓期间高点。
- 预测与研究员观察已新增稳定事件去重：同一股票/策略或形态/参考日重复运行报告不会虚增样本数。观察计划必须在 5 个交易日内真实触发并有 10 个后续交易日，才能进入正期望学习；未触发、方向不明和旧版样本会隔离。
- A 级买入信号已增加统一历史证据门槛：没有当前股票+策略的真实正期望记录时，最高只能为 B 级小仓验证；硬止损/减仓类风险退出不受该买入门槛限制。
- Alpha 测试文件已补齐可执行入口，新增“修改未来数据不得改变过去评分”、可靠性收缩、新闻半衰、零权益、事件去重和未触发隔离测试；旧数据库自动迁移与去重已通过兼容性验证。
- Tab3 盘中/盘前报价已改为逐股闸门：保留数据源已提供的 `open/high/low/volume` 和时间戳，每只股票独立检查报价是否存在、是否过期、OHLC 是否完整。任一单股缺价/过期时，该股只能回退显示 T-1 条件计划并以 D 级阻断执行，不会被组合中其他股票的实时价“带着通过”。
- 分析结果已新增 `decision_df`：历史 `df` 仍用于回测/后台优化，含实时临时 K 线的 `decision_df` 统一供当前信号、Alpha、技术标记、支撑/锁利观察和报告展示使用，避免“策略看实时、报告显示昨收”。
- 策略健康度样本已改为直接从本次 `signal_check` 写入：buy 按看多验证，sell/减仓按“退出后是否避免下跌”看空验证，C/D 级不进入可执行策略学习。Tab1 和 Tab3 均会写入逐股+逐策略样本，不再使用回测最后一笔交易猜测当前信号。
- 信号排序已移除“理由文本越长置信度越高”的占位逻辑，改为检查触发价、止损、止盈、失效条件和持仓规模等结构化字段；止盈接口已拆分为固定、动态、条件和无主动止盈四类。L/M 固定百分比目标进入 `Decision -> Order -> Position -> Broker` 并按真实成交价撮合；动态/条件止盈展示真实公式或退出条件，不伪造固定风险收益比。
- 操作方案仓位不再只按多空状态使用固定 40%/70% 上限，现由 1%/2% 单笔净值风险预算、近 20 日波动率、用户已有单票集中度和日均成交额容量共同裁剪。
- A-H/O 量化策略的未触发诊断已迁入策略自身，中央信号模块不再按策略字母硬编码或把布林突破误诊为单纯百分位不足；新增策略可通过统一 `diagnose_no_signal()` 接口提供真实条件。
- 滚动百分位改为等价的向量化 rolling rank，500 根 K 线本地连续计算 120 次约 0.04 秒；回测滑点在基础值之上按 T 日已知的历史波动率动态上调，高波动标的不再固定使用 0.3%。缺失成交量视为未知而非停牌，只有明确零成交量或停牌标记才拒绝撮合。
- 20 个注册策略已全部统一为原生 Decision-first 路径：A-O/I-N 的策略逻辑直接返回 `StrategyDecision`，策略文件不再构造 `Order`，公开订单只能由 `StrategyDecision -> decision_to_orders()` 在边界层生成；全部 20 个策略均显式覆盖 `diagnose_no_signal()`。I-N 已补齐金叉、RSI、反弹确认、趋势回调、关键反转和均线粘合的真实缺失条件，P-T 直接复用自身结构化 Decision 的触发/缺失条件，不再输出笼统占位文案。
- 修复趋势回调策略 L 的退出条件：近 10 日低点改为只统计当前交易日前的 10 根 K 线，避免把当前收盘包含在最低值中导致“跌破近10日低点”永远无法成立。
- 卖出/减仓信号已新增独立退出质量复盘：记录真实执行口径下后续 1/3/5/10/20 日涨跌、20 日最大继续下跌/反弹、避免损失、过早退出机会成本，并分为 effective/premature/neutral。Tab1 报告和 Tab3 历史评估面板均展示退出统计。
- 策略健康度已按 buy/sell 拆分。买入健康度继续控制参数晋升和回滚；卖出健康度必须等待 20 日退出复盘完成，并使用“避免损失 - 机会成本”评价退出规则，只影响 sell 信号排序，不会误删有效的入场参数。
- 策略审计已加入样本外循环分块 Bootstrap：默认 400 次，输出正期望概率、收益和夏普 95% 区间、最大回撤 P95、30% 回撤概率。样本不足不输出伪概率；正期望概率不足或收益下界过低会把 PASS 降级，20 个策略本地审计增量约 0.17 秒。
- 新股/代码复用的数据边界已收紧：股票元数据缓存权威上市日期，所选回测窗口早于上市日时，Tab1/Tab3 的缓存读取、增量拉取、指标和回测均自动从上市日开始；上市前伪K线不参与计算，新股自然样本不足会单独解释并继续执行降级闸门。
- Tab1 盘中/盘前无 T-1 缓存时已移除重复 LLM：中间 T-1 上下文由确定性模板生成，仅最终报告调用一次 LLM；保留完整事实输入和最终研究员解读，减少一次最长 9000 token 的串行等待。
- Windows PyInstaller 构建已补齐 setuptools/pkg_resources 的 jaraco 动态依赖、akshare 的 jsonpath/交易日历数据和报告导出的 markdown_it；同时取消 Torch 运行所需标准库 unittest 的错误排除。本地 bat 与 GitHub Actions 会在上传产物前实际启动 EXE 执行运行时依赖烟雾测试。
- Tab1/Tab3 新闻链路已解耦：两页共用“新鲜度检查→主动拉取→仅分析新增项→写回缓存”服务，Tab3 在自身并发预取中独立刷新。新闻新增 published_at/fetched_at 和逐股刷新状态；盘中/盘前/盘后 TTL 分别为 30 分钟/1 小时/6 小时，刷新失败的历史缓存不进入当次 Alpha。
- 因子验证未知态不再被误判为方向错误并半权处理：样本不足时保留研究先验权重，报告明确显示“未验证”及整体覆盖率；验证覆盖低于 50% 时进入数据质量观察闸门、压缩仓位并降低执行等级。
- 回测止损撮合已补齐跳空约束：盘中穿越止损按止损价，开盘已跳空越过止损则按更差的开盘价成交，避免高波动标的回测收益被系统性高估。条件/风控覆盖策略改由策略元数据自动发现，同板块名称查询和已确认死代码同步修复。
- Tab3 组合估值已改为同一批冻结现价同时计算账户权益、持仓市值和仓位，避免预取失败时“成本价分母+现价分子”产生超过100%的伪仓位。SQLite 改为每线程独立连接、写操作全局串行；并发预取失败还会串行补齐 K 线、基本面和新闻整包。
- Tab1/Tab3 报价调用已按时段和供应商配额治理：美股盘前/盘后直接走 Nasdaq.com -> yfinance，不再先请求或失败后回退 TickFlow；常规盘中只使用 TickFlow，Tab3 多股票通过批量接口一次获取，批量缺失时逐股阻断而不拿延伸时段价格冒充盘中价。Nasdaq 首次刷新限制为 2 路，Finnhub/新闻/基本面预取限制为 2 路；基本面失败缓存从整次进程缩短为 5 分钟。
- TickFlow 组合盘中报价已按服务端“单次最多5只”限制自动拆批，并把单轮请求控制在免费额度 10 次以内；研究员系统确认表保留完整观察原文。Tab3 系统追踪按当前组合成分股和模式统计，整体正确率按股票、依据日、方向和动作去重为独立事件，不再让同一机会的多个策略重复放大样本数。
- Tab3 历史预测评估已新增独立的美股/A股切换；面板按市场纳入全部 Tab1/Tab3 预测代码，不再要求股票必须先进入组合持仓或关注列表，并同时展示已验证、待验证和不可验证数量。
- 系统追踪面向用户改为天气预报式“历史方向预测结果”：每个独立事件直接写明何时预测、预测哪个目标交易日、以当时价格为基准预测上涨还是下跌、目标日实际收盘价和最终对错。报告明确说明当前能力是预测目标日收盘方向而非精确目标价，并公开±1%的涨跌/震荡判定标准；逐策略入场/退出样本仍保留在数据库供健康度学习。PDF 宽表单元格可自动换行并清理原始 `<b>` 标签。
- 持仓退出新增策略共识仲裁：Q/R/S 专用持仓风控可直接进入退出层，普通策略至少需要两个独立策略共同确认；单个普通策略退出只显示为分歧复核。横向回测表改为“历史策略适配”，禁止用最高历史收益直接称某股票为最优质资产；同触发价的保守/激进方案明确只在仓位和风险预算上不同。
- 趋势策略 O 已明确降为“仅回测基准”：继续参与历史横向比较和审计，但通过策略元数据从盘前/盘中/盘后当前信号、操作方案和预测学习路径中统一隔离。Tab3 的组合新增容量/可用资金闸门提前到一分钟操作台和条件触发表；容量为0时策略买入只保留为候选，不再先显示可执行、最后才分配0元。可信摘要明确使用“当前机会关联历史验证”口径，组合 LLM 在缺少止盈目标时不得评价风险收益比优劣。
- Tab1/Tab3 止盈展示已结构化：固定目标显示目标价和真实风险收益比，移动止盈显示最高价/ATR公式，条件止盈显示反向信号或技术退出条件，仅止损策略明确暴露“无主动止盈”。盘后、盘前、盘中和组合 LLM 均被约束为只能解释该结构，不得补造目标价。
- LLM 报告已增加确定性输出合规层：重复的“研究员观察候选”标题会在入库和导出前归一化为一个；LLM 对风险收益比的主观夸赞会被中性化，解释正文中的 `1:x` 会统一改为引用代码方案，具体数值只允许出现在代码生成的确定性操作表中，避免跨股票误配。
- 2026-07-05 完成 P0/P1 二次可信度审查修复：联合 OOF 漂移限定同版本，交易方案增加实际决策会话和物化参数版本键，LLM 历史反馈禁止越过事实/止损闸门；模型晋升加入配对时间块 Bootstrap 与 ECE 约束，策略健康度的胜率和 Wilson 下界按证据质量加权；联合 OOF 报告分开显示策略 Decision 与 Broker 成交，趋势回调/锁利/MA120 形态关键词取消宽泛误判。
- 2026-07-05 完成 P0/P1 复审补强：3/5日模型选择与确认窗口加入标签成熟隔离带；D级数据冲突只能维持或降级，历史反馈不得抬升；5到7个薄样本即使全胜也只能观察。交易方案事件键加入策略版本和账户物化快照，历史面板按证据质量跨日加权；预测概率与 P10/P50/P90 改为同源条件分布并把区间覆盖率纳入晋升。模型与联合策略升级到 v4，旧 Champion 自动退出等待重新验证。

五阶段当前完成度（2026-07-05 P0/P1 可信度升级落地后）：

| 阶段 | 完成度 | 当前状态 |
|------|------:|------|
| 第一阶段：条件触发计划 | 100% | 报告已输出触发/失效/固定、动态、条件止盈和动态仓位风控；固定目标真实计算风险收益比，Tab3 逐股报价、实时 OHLC、组合新增容量和可用资金均在首屏进入硬闸门；逐股重复策略表已从组合正文移除，HTML 长表默认折叠 |
| 第二阶段：升级 strategies/ | 100% | 20 个注册策略已统一原生 Decision-first 路径和固定/动态/条件止盈语义；策略 O 仅作回测基准，全部策略覆盖未触发诊断；执行候选和普通退出共识已按独立策略家族去重，避免相似策略重复放大观点 |
| 第三阶段：LLM 观察候选池 | 92% | 已有候选解析、六类结构化形态、系统确认、触发算子、事件去重、LLM/系统命中率分账、历史自动降级和报告展示；历史正期望不能越过当前事实/止损闸门；剩余是独立的全局筛选和单事件钻取交互 |
| 第四阶段：历史验证与自我升级 | 100% | 独立预测、四类受控 Challenger、标签成熟隔离、双窗口 OOF、配对时间块 Bootstrap/ECE/区间覆盖晋升、概率收益同源分布、逐股 v4 Champion、在线回滚、实际决策会话和证据质量跨日加权置信区间、薄样本观察闸门、方案双账验证、多模型多周期联合 OOF、同版本漂移、Decision/Broker 分账、校准图、point-in-time 快照和前瞻分钟复盘均已落地；真实证据继续自然积累 |
| 第五阶段：数据质量和市场规则 | 96% | 已有数据质量闸门、缓存污染防护、上市日期裁剪、交易所会话过滤、独立分钟存储与证据来源审计、逐股报价校验、动态滑点、跳空止损、A股盘中 T+1/费用及组合流动性约束；仍需可靠停复牌/ST 数据和可选授权 Level 2 适配器 |

后续 P2/体验重点：

- 历史预测评估 UI 的全局日期筛选、单事件点击钻取和跨周期分歧筛选。
- point-in-time 新闻/基本面和真实分钟证据达到覆盖阈值后，再通过新一轮 OOF 决定是否值得加入正式预测特征；不能仅因有数据就启用。
- 市场规则仍需接入可靠停复牌/ST 标识，并可按用户订阅增加美股授权 Level 2 数据适配器。
- Web 版完善与安装包体积优化。
